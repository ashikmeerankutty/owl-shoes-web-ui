import { Box, Text } from "@twilio-paste/core";

export const ScheduleMeetingConfirm = () => {
    return (
        <Box>
            <Text as="p">Select “Confirm” to schedule meeting.</Text>
        </Box>
    );
};
